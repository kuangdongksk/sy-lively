import { OptionsHelper } from "@/class/OptionsHelper";
import { Form, Select, TimePicker } from "antd";
import { useState } from "react";

export interface ICron输入Props {}

function Cron输入(props: ICron输入Props) {
  const {} = props;

  const [cron, 令Cron为] = useState<string[]>();

  return (
    <Form>

    </Form>
  );
}
export default Cron输入;
